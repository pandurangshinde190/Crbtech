#include<stdio.h>
/*
struct Employee
{
	int id;
	float sal;
	char name[50];
};

typedef struct Employee Emp;

int main(void)
{
	Emp e1 = { 2121, 100000, "Yash" };
	Emp* ptr; //"ptr" is a "pointer to Emp"

	ptr = &e1;

	printf("\nID of e1 is : %d",e1.id);
	printf("\nID of e1 is : %d", ptr->id);
	printf("\nID of e1 is : %d", (*ptr).id);

	return 0;
}
*/