#include<stdio.h>
/*
void calculateAreaPerimeter(int );

int main(void)
{
	int radius;

	printf("\nEnter the value for radius  : ");
	scanf("%d",&radius);

	calculateAreaPerimeter(radius); //actual parameter
	return 0;
}

void calculateAreaPerimeter(int r)  //r is formal parameter
{
	//This function is supposed to calculate the "area" and "perimeter"
	//of the circle and display it
	//we require the "VALUE OF RADIUS" for this

	float area;
	float perimeter;

	//area = 3.14f * radius * radius;

	area = 3.14f * r * r;
	perimeter = 2 * 3.14f * r;

	printf("\nArea of Circle is : %f sq. Units",area);
	printf("\nPerimeter of Circle is : %f ", perimeter);

}
*/