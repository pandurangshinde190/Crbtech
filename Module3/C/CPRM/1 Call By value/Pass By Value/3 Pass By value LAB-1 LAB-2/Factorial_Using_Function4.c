#include<stdio.h>
/*
void calculateFactorial(int);
void displayNoAndItsFactorial(int ,int );
void acceptAnInteger(void)  //Function Defination as a result of which 
							// Function Declaration is also present in it
{
	int no;

	printf("\nEnter an integer : ");
	scanf("%d", &no);

	calculateFactorial(no);
}

int main(void)
{
	
	acceptAnInteger();
	return 0;
}

void calculateFactorial(int no)
{
	int i;
	int fact;

	i = 1;
	fact = 1;
	while (i <= no)
	{
		fact *= i;
		++i;
	}

	displayNoAndItsFactorial(no, fact);

	//printf("\nFactorial of %d is  %d ",no,fact);
}

*/