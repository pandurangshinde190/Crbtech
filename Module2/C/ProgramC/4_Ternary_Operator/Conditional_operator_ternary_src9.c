#include<stdio.h>
int main(void)
{
    int a =10, b= 20, c =20, d = 20;
    int x = 0, y = 1;
    int Result;

    /*
     // printf("\n\n%d",b); //20
        Result = (a>b) ? (a+b+c) : (b=54);
     // printf("\n\n%d",b); //54
        printf("\n\nValue of Result is = %d",Result);
   */
        /*
        Result = (!a) ? (a !=10) : (a&&b);
        printf("\n\nValue of Result is = %d",Result);  //1
        */
        
        /*
        Result = (printf("Hello")) ? (a!=b) : (++b);
        printf("\n\nValue of Result is = %d",Result);  //1
        */
       
       /*
       int a =printf("Yash");
       printf("%d",a);
       */ 

       /*
        Result = (a+b+c) ? (!!0) : (a!=b);
        printf("\n\nValue of Result is = %d",Result); //0
       */
       
      
        /*
        Result = (c&&d) ? (a||b) : (b&&a);
        printf("\n\nValue of Result is = %d",Result);//1
        */
        

        /*
        Result = (!++a) ? (a) : (printf("Love you"));
        printf("\n\nValue of Result is = %d",Result);   //Love You
        */
        

    return 0;
}