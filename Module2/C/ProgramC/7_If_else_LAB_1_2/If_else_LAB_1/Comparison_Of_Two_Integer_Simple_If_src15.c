#include<stdio.h>
int main(void)
{
    int a, b;

    printf("Enter the value for a :");
    scanf("%d", &a);
    printf("Enter the value for b :");
    scanf("%d",&b);

    /*
    if(a > b)
    {
        printf("\na is greter than b ");
    }
    
    if(b > a)
    {
        printf("\nb is greter than a ");
    }

    if(a == b)
    {
        printf("\na and b both are equal");
    }

    if(a != b)
    {
        printf("\na is not equal to b");
    }
    */

    /*
    if(a > b)
        printf("\na is greter than b ");
    
    
    if(b > a)
        printf("\nb is greter than a ");

    if(a == b)
        printf("\na and b both are equal");

    if(a != b)
        printf("\na is not equal to b");
    */

    return 0;
}